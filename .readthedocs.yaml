version: 2

build:
  os: ubuntu-22.04  # Lebih stabil untuk CI3
  tools:
    python: "3.9"   # Python versi compatible

sphinx:
  configuration: docs/conf.py
  builder: html
  fail_on_warning: false

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

formats:
  - html
  - pdf

# Build dari folder docs
sphinx:
  configuration: docs/conf.py